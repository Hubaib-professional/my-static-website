name: Create Environments

on:
  workflow_dispatch:

jobs:
  create-development:
    runs-on: ubuntu-latest
    environment:
      name: development
    steps:
      - run: echo "Creating development environment"

  create-staging:
    runs-on: ubuntu-latest
    environment:
      name: staging
    steps:
      - run: echo "Creating staging environment"

  create-production:
    runs-on: ubuntu-latest
    environment:
      name: production
    steps:
      - run: echo "Creating production environment"